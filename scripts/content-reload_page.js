// This script reloads the page, which helps reset the page when the extension is shut off

(() => {
  console.log('DISABLED - initiating content reload script');

  // window.location.reload();
})();
